<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title%>
    </title>
    <style>
        body {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-style: italic;
            font-family: fantasy;
        }

        .box {
            margin: auto;
            border: 2px solid black;
            display: flex;
            flex-direction: column;
            flex-wrap: wrap;
            padding: 34px;
        }

        div {
            padding: 13px;
        }

        input,
        div {
            display: inline;
            /* grid-template-columns: 1fr 2fr; */
        }

        button {
            width: fit-content;
            margin-left: 23px;
        }
    </style>
</head>

<body>
    <form action="/" method="post" class="box">
        <div>name :<input type="text" name="username" id="user1"></div>
        <% if(data.username) {%>
            <span><%=data.username.msg%></span>
            <%} else {%>
            <%}%>
        <div>password :<input type="text" name="password" id="user2"></div>
        <% if(data.password) {%>
        <span><%=data.password.msg%></span>
        <%} else {%>
        <%}%>
        <div>email: <input type="email" name="gmail" id="user3"></div>
        <% if(data.gmail) {%>
            <span><%=data.gmail.msg%></span>
            <%} else {%>
            <%}%>
         <button type="submit">submit</button>
    </form>
</body>

</html>